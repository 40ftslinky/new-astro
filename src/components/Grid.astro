---
interface Props {
	variant?: 'offset' | 'even' | 'small' | 'medium' | 'large';
}

const { variant } = Astro.props;
---

<ul class:list={['flex', { offset: variant === 'offset', even: variant === 'even', small: variant === 'small', medium: variant === 'medium', large: variant === 'large'  }]}>
	<slot />
</ul>

<style>

.flex {
	display: flex;
	flex-wrap: wrap;
	list-style: none;
	padding: 0;
	margin: 0;
}



</style>
